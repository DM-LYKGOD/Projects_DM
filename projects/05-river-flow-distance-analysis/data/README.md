# Required Input Data

Add the following source files to this directory before running `src/river_flow_distance_analysis.R`:

- `coordinates.csv`
- `rivers_rlp.gpkg`
- `dtm_germany_rheinland_pfalz_20m.tif`

The script reads these files directly and writes results to the sibling `outputs/` directory.
